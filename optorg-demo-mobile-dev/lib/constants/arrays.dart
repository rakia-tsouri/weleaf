enum ConnectivityStatus { ONLINE, OFFLINE }

enum DIALOG_TYPE {
  INFO_DIALOG,
  CHOICE_SELECTOR_DIALOG,
  INPUT_DIALOG,
  TWO_BUTTONS_DIALOG,
  FINGERPRINT_POPUP,
}

// textinput formater type
enum InputFormatterType { free, double, int }
